# Convex Backend
# Get these values from https://dashboard.convex.dev after creating a project
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud
CONVEX_DEPLOYMENT=prod:your-project

# Cloudinary Configuration
# Sign up at https://cloudinary.com and get your credentials from the dashboard
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

# Authentication
# Generate AUTH_SECRET with: openssl rand -base64 32
AUTH_SECRET=generate-with-openssl-rand-base64-32

# Google OAuth (optional for MVP, required for "Sign in with Google")
# Setup instructions:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (Web application type)
# 3. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 4. Download JSON credentials or copy Client ID and Secret
# 5. Add both here AND in Convex Dashboard environment variables
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret

# Development Notes:
# 1. Copy this file to .env.local and fill in the actual values
# 2. Never commit .env.local to version control
# 3. For development, use the Convex dev deployment URL
# 4. For production, use the Convex prod deployment URL
